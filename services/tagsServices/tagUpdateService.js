const tagModel = require('../../models/tagsModel.js')

const tagUpdateService = async (tagData) => {
    try {


    } catch(error) {
        throw Error('Error while fetching user. Location: tagUpdateService')
    }
}

module.exports = {
    tagUpdateService
}
