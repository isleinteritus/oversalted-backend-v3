const tagModel = require('../../models/tagsModel.js')

const tagCreateService = async (tagData) => {
    try {


    } catch(error) {
        throw Error('Error while fetching user. Location: tagCreateService')
    }
}

module.exports = {
    tagCreateService
}
