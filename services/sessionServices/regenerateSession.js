//when user logs in
//whenever user updates their personal infomration regenerate session.
//when a user posts/updates/deletes regenerate session.
const regenerateSession = (variableName) => {
    try {

    }catch (error) {

    }
}
