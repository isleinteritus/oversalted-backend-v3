const commentModel = require('../../models/commentsModel.js')

const commentCreateService = async (commentData) => {
    try {


    } catch(error) {
        throw Error('Error while fetching user. Location: commentCreateService')
    }
}

module.exports = {
    commentCreateService
}
