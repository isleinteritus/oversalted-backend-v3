const commentModel = require('../../models/commentsModel.js')

const commentIndexService = async (commentData) => {
    try {


    } catch(error) {
        throw Error('Error while fetching user. Location: commentIndexService')
    }
}

module.exports = {
    commentIndexService
}
