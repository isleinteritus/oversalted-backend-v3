const commentModel = require('../../models/commentsModel.js')

const commentDeleteService = async (commentData) => {
    try {


    } catch(error) {
        throw Error('Error while fetching user. Location: commentDeleteService')
    }
}

module.exports = {
    commentDeleteService
}
